# LLM
GROQ_API_KEY=

# MLflow
MLFLOW_TRACKING_URI=http://mlflow:5000

# Dataset path (optional — defaults to ../data relative to backend/)
# DATA_PATH=/path/to/dataset_unificado_defasagem.csv

# Frontend (set in docker-compose, not needed at runtime for backend)
VITE_API_BASE_URL=http://localhost:8080
