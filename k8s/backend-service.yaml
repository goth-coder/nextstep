apiVersion: v1
kind: Service
metadata:
  name: nextstep-api-service
  namespace: nextstep
  labels:
    app: nextstep-api
spec:
  selector:
    app: nextstep-api
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
