apiVersion: v1
kind: Service
metadata:
  name: nextstep-web-service
  namespace: nextstep
  labels:
    app: nextstep-web
spec:
  selector:
    app: nextstep-web
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
